import { shoutcaster } from "./shoutcaster";

// maybe replace with .env or we can just commit the gameplan (config) updates
export const gameplan = {
    year: "20212022",
    gameType: "R", // see: https://statsapi.web.nhl.com/api/v1/gameTypes/
};
